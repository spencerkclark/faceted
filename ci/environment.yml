name: test
channels:
    - conda-forge
dependencies:
    - black
    - cartopy
    - jupyter
    - matplotlib
    - numpy
    - pip
    - pytest
    - scipy
    - pip:
      - coveralls
      - pytest-cov
